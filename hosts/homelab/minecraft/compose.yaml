services:
  minecraft:
    image: docker.io/itzg/minecraft-server
    tty: true
    stdin_open: true
    ports:
      - 25565:25565
    environment:
      EULA: "TRUE"
      MAX_MEMORY: 8G
    volumes:
      - /mnt/data/minecraft:/data
    restart: unless-stopped
networks: {}
